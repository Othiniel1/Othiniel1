## 2025-02-22 - Shared State Store Hydration in Next.js CSR Components
**Learning:** Instantiating reactive mock stores outside React's lifecycle in client components can cause re-render overhead if state updates trigger full object copies without memoized selector hooks.
**Action:** Use localized subscriber patterns and lightweight event listeners for client-side state sync across multi-portal prototypes.
